public class Ex1 {
    static void testMod()
    static void testIfElse(int x)
    static int [] generateScores(int n) {
        Random random = new Random();
        int x[] = generateScores(50);
        for (int i = 0; i < x.length; i++){
            sum += x[i];
        } 
        double average = sum / (double)
        System.out.println("sum is"+ sum);
    }
}
